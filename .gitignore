slop.db
slop.db-wal
slop.db-shm
node_modules/
.DS_Store
.claude/

slop.config.json

# edge-function secrets — never commit real keys (only .env.example is tracked)
supabase/functions/.env
supabase/.env

# Supabase CLI local cache
supabase/.temp/

# Generated web output and local verification artifacts
dist/
.art-export/
*.local
